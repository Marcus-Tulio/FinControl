/** Classe aplicada em document.documentElement quando o usuário é perfil Empresa (ver .theme-business
 * em globals.css) — alcança inclusive conteúdo portalizado (Popover, Dialog, Sheet) e se comporta
 * corretamente em modo claro/escuro pela cascata normal do CSS. */
export const BUSINESS_THEME_CLASS = "theme-business";
